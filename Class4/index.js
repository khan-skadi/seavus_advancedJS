// se so e zavisno od povratnata data go stavame vo callback funkcija.
// hoisting mechanism(v8 engine). All regular functions can be called before they are defined. V8 engine reads all function definitions first. Anonymous functions must be called after they are defined.
